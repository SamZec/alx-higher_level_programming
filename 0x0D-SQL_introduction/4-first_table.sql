-- 4-first_table.sql
-- a script that creates a table called first_table in the current database in your MySQL server.

-- Create table fist_table with field id: INT and name: VARCHAR(250)
 CREATE TABLE IF NOT EXISTS first_table (id INT, name VARCHAR(250));
